---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  pageTitle: string;
  lastUpdated?: string;
}

const { title, description, pageTitle, lastUpdated } = Astro.props;
---

<BaseLayout title={title} description={description} showNavLinks={false}>
  <section class="legal-hero">
    <div class="container">
      <h1 class="legal-hero__title">{pageTitle}</h1>
      <span class="legal-hero__accent" aria-hidden="true"></span>
      {lastUpdated && (
        <p class="legal-hero__date">Last updated: {lastUpdated}</p>
      )}
    </div>
  </section>

  <section class="legal-content">
    <div class="container">
      <div class="legal-prose">
        <slot />
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .legal-hero {
    padding-top: calc(var(--nav-height) + var(--space-4xl));
    padding-bottom: var(--space-2xl);
  }

  .legal-hero__title {
    font-size: 2.25rem;
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: var(--color-text-primary);
  }

  .legal-hero__accent {
    display: block;
    width: 72px;
    height: 2px;
    margin-top: var(--space-md);
    background: linear-gradient(90deg, var(--color-blue), #7B2FE0);
    opacity: 0.6;
    border-radius: 1px;
  }

  .legal-hero__date {
    margin-top: var(--space-md);
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  .legal-content {
    padding-bottom: var(--space-4xl);
  }

  .legal-prose {
    max-width: 720px;
  }

  .legal-prose :global(h2) {
    margin-top: var(--space-3xl);
    margin-bottom: var(--space-md);
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--color-text-primary);
  }

  .legal-prose :global(h3) {
    margin-top: var(--space-2xl);
    margin-bottom: var(--space-sm);
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--color-text-primary);
  }

  .legal-prose :global(p) {
    margin-bottom: var(--space-md);
    color: var(--color-text-secondary);
    line-height: 1.75;
  }

  .legal-prose :global(ul),
  .legal-prose :global(ol) {
    margin-bottom: var(--space-md);
    padding-left: var(--space-lg);
    color: var(--color-text-secondary);
    line-height: 1.75;
  }

  .legal-prose :global(li) {
    margin-bottom: var(--space-xs);
  }

  .legal-prose :global(a) {
    color: var(--color-blue-light);
    text-decoration: underline;
    text-decoration-color: rgba(92, 125, 255, 0.4);
    text-underline-offset: 3px;
    transition: color var(--transition-base), text-decoration-color var(--transition-base);
  }

  .legal-prose :global(a:hover) {
    color: var(--color-blue);
    text-decoration-color: var(--color-blue);
  }

  .legal-prose :global(strong) {
    color: var(--color-text-primary);
    font-weight: 600;
  }
</style>
